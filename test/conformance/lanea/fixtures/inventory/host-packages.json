{
  "_comment": [
    "A host package inventory in internal/collector/host's own JSON shape.",
    "It is a FIXTURE and not a recording: no dpkg, rpm or apk exists on the",
    "machine this harness runs on, so host.Collect cannot produce one here and",
    "the harness says so rather than pretending. The keys are host.Package's",
    "JSON tags (ecosystem/package/version/arch), so a change to that struct",
    "breaks this file rather than silently ignoring it.",
    "",
    "NOTE WHAT IS MISSING: there is no purl field, because host.Package has no",
    "purl field. That is not an omission in this fixture; it is the seam the",
    "harness reports."
  ],
  "packages": [
    {"ecosystem": "apk", "package": "openssl", "version": "3.1.3-r0"},
    {"ecosystem": "apk", "package": "busybox", "version": "1.36.1-r16"},
    {"ecosystem": "rpm", "package": "python3-requests", "version": "2.25.1-3.el9", "arch": "noarch"},
    {"ecosystem": "deb", "package": "openssl", "version": "1.1.1n-0+deb11u3", "arch": "amd64"}
  ]
}
